function solve(firstNumber, secondNumber, thirdNumber){
console.log(`The largest number is ${Math.max(Number(firstNumber), Number(secondNumber), Number(thirdNumber))}.`);
}

solve(5, -3, 16);
solve(-3, -5, -22.5);