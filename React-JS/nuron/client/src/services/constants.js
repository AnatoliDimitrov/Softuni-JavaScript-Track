const BASE = 'http://localhost:3005/';
const NFTS = `${BASE}nfts`;
const USERS = `${BASE}user`;
const USERS_REGISTER = `${BASE}user/register`;
const USERS_LOGIN = `${BASE}user/login`;
const LOGOUT = `${BASE}user/logout`;
const USERS_EDIT = `${BASE}user/edit`;
const USERS_EDIT_PICTURE = `${BASE}user/edit-picture`;

export default {
    BASE,
    NFTS,
    USERS,
    USERS_REGISTER,
    USERS_LOGIN,
    USERS_EDIT,
    LOGOUT,
    USERS_EDIT_PICTURE
}